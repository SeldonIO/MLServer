[tox]
isolated_build = true

[testenv]
allowlist_externals = poetry
commands_pre =
    poetry sync --no-root
    poetry sync -C {toxinidir}/../../
commands =
    python -m pytest {posargs} -n auto \
        {toxinidir}/tests
