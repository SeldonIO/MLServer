# LLM API wrapper runtime for MLServer

This package provides a MLServer runtime that wraps LLM access via API (e.g. OpenAI).

## Usage

You can install the runtime, alongside `mlserver`, as:

```bash
pip install mlserver mlserver-llm
```
